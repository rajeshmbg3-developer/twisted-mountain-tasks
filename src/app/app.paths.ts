/**
 * Paths that will be used only for the routes in the app
 * e.g. - { path: APP_PATHS.LOGIN, component: ExampleComponent }
 */

export const enum APP_PATHS {
  EPMTY = '',
  AUTH = 'auth',
  LOGIN = 'login',
  TEST_PATH = 'test-path',
  PAGES = 'pages',
  EXCEL_SHEET = 'excel-sheet',
  USERS = 'users',
}
